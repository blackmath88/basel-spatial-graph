# Runtime (serving the API and the map)
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
networkx>=3.2
numpy>=1.24
shapely>=2.0
pyproj>=3.4
pydantic>=2.8

# Data preparation only (`python -m app.prepare_data`).
# Pulls in geopandas/pandas; the running server never imports it.
osmnx>=2.0

# Tests
pytest>=8.0

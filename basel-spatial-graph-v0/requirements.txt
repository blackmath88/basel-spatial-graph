# Runtime: serving the API, the map, the CLI and the MCP tools.
# This is all the tested quick-start path needs — the repository ships the
# prepared data, so nothing here downloads or rebuilds anything.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
networkx>=3.2
numpy>=1.24
shapely>=2.0
pyproj>=3.4
pydantic>=2.8

# Tests
pytest>=8.0
